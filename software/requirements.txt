# WARLOCK - Two-Pi System Dependencies
#
# This file installs dependencies for BOTH units.
# Use this for development on a single machine.
#
# For production deployment on separate hardware:
# - Helmet Pi: pip install -r helmet/requirements.txt
# - Body Pi:   pip install -r body/requirements.txt

# Common dependencies (both units)
-r common/requirements.txt

# Helmet-Mounted Unit (HMU)
-r helmet/requirements.txt

# Body-Mounted Unit (BMU)
-r body/requirements.txt

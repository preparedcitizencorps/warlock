# WARLOCK Helmet-Mounted Unit (HMU) Dependencies

# Camera and computer vision
opencv-python>=4.8.0
numpy>=1.24.0

# Machine learning / AI
ultralytics>=8.0.0  # YOLO11n

# Configuration
pyyaml>=6.0

# HTTP for map tile downloads
requests>=2.31.0

# DRM/KMS display and evdev input (for headless mode)
# Note: python3-kms++ must be installed via apt (not pip)
# Install with: sudo apt install python3-kms++
evdev>=1.6.0  # Keyboard input without X11

# Optional: Hailo AI accelerator support (when hardware available)
# hailo-platform  # Install from Hailo SDK

# Optional: IMU sensor (BNO085)
# adafruit-circuitpython-bno08x

# Optional: GPS module
# gpsd-py3  # For U-blox NEO-M9N backup GPS
